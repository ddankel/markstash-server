const components = {
  parameters: require("./parameters"),
  responses: require("./responses"),
  requestBodies: require("./requestBodies"),
  schemas: require("./schemas"),
};

module.exports = components;
